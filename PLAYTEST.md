# 三局试玩监控说明

## 目的

这轮不凭感觉直接改数值，而是用三局真实操作回答四个问题：升级速度是否合适、三种武器是否存在明显弱势、两场 Boss 是否过密或过慢、掉帧是否由敌人或弹幕峰值造成。

监控仅在 localhost/127.0.0.1 且 URL 含 `telemetry=1` 时启用。数据保存在当前浏览器的 `localStorage`，不会上传，也不记录键盘轨迹、鼠标轨迹或个人信息。

## 三局协议

使用正式数值，每局保持相同的流派选择和禁用项，只改变初始武器：

1. 脉冲机炮；
2. 腐蚀孢子；
3. 相位刃。

每局正常玩到胜利或死亡。若主动结束，请先暂停再按 Q 返回菜单，使该局记为 `quit`；直接关闭或刷新页面会在下一次启动时记为 `abandoned`，不计入三局有效结果。

试玩入口：

```text
http://127.0.0.1:8765/?telemetry=1&session=three-run-balance&runs=3
```

汇总页面：

```text
http://127.0.0.1:8765/dev/run-monitor.html
```

## 记录指标

- 回合结果、有效局时、武器、流派、等级、击杀和分数；
- 实际受伤、护盾抵消次数及伤害来源；
- 敌人和敌弹峰值、平均 FPS、最低 FPS、低于 45 FPS 的帧占比；
- 每两秒的生命、等级、形态、敌人数、敌弹数和当前 Boss 阶段；
- 中期/最终 Boss 首次出现、阶段停留时间、击败时间和战斗耗时；
- 属性选择、技能选择和形态进化的时间点。

## 调整判据

- 三局都在中期 Boss 前死亡：优先检查前 90 秒生成压力与升级速度，不先削 Boss。
- 只有一种武器明显更慢或受伤更多：检查该武器成型速度、射程和清场能力。
- Boss 战耗时过长但普通阶段正常：调整 Boss 生命、减伤区或输出窗口。
- 敌弹峰值与受伤同步突增：调整弹幕间隔或安全缺口，不简单增加玩家生命。
- 最低 FPS 偶发下降但低帧占比很低：先视为单帧尖峰；持续低帧才考虑对象池或弹幕上限。
- 三局胜利时间都明显短于目标：先延后事件或增加成长段落；不要用单纯加血拖时长。

三局样本只能用于发现明显问题和确定下一轮方向，不能证明正式平衡已经完成。

## 第一局后的版本说明

- 第 1 局：`v33` 基线，机炮弹幕流；3:02 通关，棱镜 8 秒、星蚀龙 2 秒，暴露敌方数量/生命不足、场外经验和瞬时转向问题。
- 第 2~3 局：`v34-feedback-1`，用于验证第一轮修正以及 Nova/Sword 的实际手感。
- 因中途发生平衡修订，这三局不能直接作为最终横向武器排名；若 v34 方向成立，需要再补一局 v34 机炮对照。
