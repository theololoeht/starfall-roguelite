# GLM 开发交接：战机流派与特效逻辑

## 你的职责与边界

你负责补充其他流派的玩法代码和对应表现接线，但必须遵守本文件的视觉语法。不要重写现有公共美术管线，不要改变已经验证的弹道坐标约定。

优先阅读：`DESIGN.md`、`BALANCE.md`、`js/attacks.js`、`js/fx.js`、`js/sprites.js`、`js/weapons.js`、`tests/attack-framework-smoke.mjs`。

当前已经完成并应复用：

- `Sprites.playerMuzzles(formId, count, lv)`：可见炮口与子弹生成位置的唯一来源。
- `Sprites.playerExhausts(formId)`：喷口与尾焰数量的唯一来源。
- `FX.energyPlume(...)`：Canvas 2D 的多层域扭曲尾焰。
- `Player.muzzleFlashT`：开火瞬间的枪口闪光状态。
- 子弹局部坐标：飞行前方永远是 `+x`，拖尾永远向 `-x`；不能二次旋转。
- `activeWeaponStats(player, weapon)`：已接蜂巢树时读取树计算结果，未接树时回退 `levelStats`。
- `FX.telegraphRing(...)`：前摇阶段使用的分段蓄能环，`progress` 必须从 0 单调增长到 1。
- `FX.arcBlade(...)`：刀口外晕、主色与白芯三层弧线，视觉半径必须使用真实判定半径。
- `FX.slashMark(...)`：相位刃命中裂口，不要用普通爆炸替代。
- `FX.shieldField(...) / FX.shieldImpact(...)`：一次性光盾的待机层数与方向受击反馈；不得改回无方向的普通爆炸环。

## GLM 接入方式

1. 在 `WEAPON_DEFS` 定义基础数值和武器类型；需要正式开放时，在 `SKILL_TREES` 增加同 id 的树，并在 `js/weapons.js` 末尾通过 `registerAttackHandler` 注册攻击契约。
2. 树的基础参数放在 `forms.base.fire`，战斗逻辑通过 `activeWeaponStats(p, w)` 读取，禁止在表现层复制伤害或半径。
3. handler 的 `update` 只推进状态和执行判定：`windup → active → cooldown`；`draw` 只读取同一 `AttackRuntime` 绘制，不能在 draw 中造成伤害。
4. 前摇进度写成 `1 - remaining / total`；伤害发生的帧才允许白热核心达到最高亮度。
5. 机体专属造型放在 `Sprites.player`，通过 `treeId` 选择；颜色来自技能树 form，避免 UI、机体和技能各写一份色值。
6. 新增流派后补 smoke test，至少验证菜单开放、前摇不提前伤害、攻击峰值造成一次确定性伤害、视觉半径与判定半径同源。
7. 同步扩展 `UI.syncHUD` 的类型化面板；禁止默认每种武器都有 `damage` 和 `projectiles`。范围、持续伤害、近战弧度等必须显示自己的核心指标。
8. 完整进化线使用 `branchForms: [leftId, rightId]` 与 `finalForm` 描述结构；基础形态的 `evolutions` 指向两个分支，两个分支各自再指向同一个终极形态。不要在 `game.js` 写死业务形态名。

已实现范例：

- `nova/base`：特殊的 `bodyScatter + proximityArm` 漂浮陷阱；在机体周围随机长出，敌机进入 190px 后才缓慢追踪，存活上限为 12。命中时外壳碎裂，原地展开 54px、3 秒的孢子云；视觉重点是悬浮、膨胀和菌尘扩散，不是高速弹道。
- `nova/flameblade`：前向连续焰刃；`tick` 用线段距离判定，`draw` 使用同一组 range/width 调用 `FX.flameBlade`。
- `nova/mist`：世界坐标驻留软粒子；粒子只负责视觉，伤害按同半径圆形领域确定性结算。
- `nova/plague`：终极组合态；双旋焰刃、雾域与周期坍缩同时存在，但仍分别复用前两条分支的判定函数。
- `nova/plague` 必须读取 `parentFormId`：毒雾来源为自动公转双刃，焰刃来源为主动锁敌双刃；表现层读取 `AttackRuntime.bladeBehavior/bladeAngles`，禁止自行猜测路径。
- `sword`：`swordTick` 负责定向前摇、弧形扫描和同次挥刀命中去重；`drawWeaponFx` 绘制刀口与命中裂口；满树后为 360° 双层余光。

## 游戏的视觉语言

| 语义 | 主色 | 形状和节奏 |
|---|---|---|
| 玩家、精确、基础能量 | 青蓝 `#4fd2ff` | 细线、尖头、稳定脉冲 |
| 散裂、数量、碎片 | 品红 `#ff5de3` | 分叉、菱形、短促多点闪光 |
| 蓄能、贯穿、重击 | 金色 `#ffd25d` | 长轴、同心环、白热核心 |
| 腐蚀、持续伤害 | 绿青 `#5dffd2` | 扩散环、残留带、缓慢衰减 |
| 冲撞、爆炸、动能 | 橙色 `#ff9d5d` | 压缩前摇、宽冲击环、碎屑 |
| 相位刃、近战 | 紫色 `#b77dff` | 清晰弧线、切口闪白、快速消失 |
| 敌方危险 | 红色系 | 预警必须先于伤害，不能与玩家青色混淆 |
| 最高温/最高能量 | 白色 | 只能作为核心或极短峰值，不能大面积常亮 |

## 一个合格特效的时间结构

1. 前摇：告诉玩家攻击将从哪里发生。蓄能环、压缩尾焰或方向线，通常占动作总时长 15%–30%。
2. 爆发：真正造成伤害的帧必须最亮、最清晰，白芯只在这里出现。
3. 余波：冲击环、粒子和尾迹说明力量方向，但不得遮挡下一次危险预警。

表现必须服从判定：视觉宽度不能明显小于碰撞区；无伤害装饰应更暗、更透明，避免玩家误判。

## 各流派的特效任务

### 冲撞装甲

- 前摇：机头方向压缩出两道橙色收束线，尾焰缩短后突然拉长。
- 动作：冲刺阶段产生方向一致的机体残影，不使用随机位置圆点冒充速度。
- 命中：接触点白闪一次，橙色宽环沿碰撞法线扩散。
- 禁止：整个冲刺过程持续大面积白光，否则看不见敌弹。

### DOT 四形态

- 腐蚀孢子：机体有多个培养孔；孢体从轮廓周围随机“长出”，像水母/种荚一样缓慢漂浮，近敌后白核苏醒但仍保持柔和转向。命中时叶片外壳向外碎裂，留下半透明菌幕、七颗绕流微孢和呼吸式边缘环；禁止画成固定炮口直射、高速导弹或近身径向脉冲。
- 连续焰刃：局部 `+x` 为喷射方向；绿外焰、橙主体、白芯三层，视觉长度和宽度必须取自判定参数。
- 粒子毒雾：使用无白热弹芯的软粒子团，粒子驻留在世界坐标，不能黏死在机体局部坐标上。
- 疫焰天灾：允许双向焰刃与雾域叠加，但白色只保留在焰刃核心，雾粒不得全变成子弹亮点。
- 持续状态：敌人只保留小型分段层数标记，不为每个敌人绘制大面积光晕。
- 恢复联动：每 `+1.0/s` 恢复换算 `+12%` 腐蚀伤害、`+5%` DOT 距离，有效恢复最多按 `5.0/s` 计算；表现层必须读取运行时结果，不能另写倍率。

### 相位刃

- 前摇：刀弧起点出现短线，不提前画完整扇区。
- 动作：白色刀口 + 紫色外晕两层；刀口宽、判定宽和持续时间必须一致。
- 命中：敌人位置出现垂直于刀弧切线的短促裂口，而不是普通圆形爆炸。

### 矢量尾焰

- 机体喷口使用 `Sprites.playerExhausts`，地面伤害轨迹继续使用现有世界坐标尾迹。
- 移动尾迹应表现为连续缎带；转弯处保持曲线，不要每帧独立生成不相连圆点。

## 绘制层级

从下到上固定为：背景 → 低亮度预警/范围 → 尾迹 → 实体 → 玩家与敌人子弹 → 主攻击白芯 → 命中粒子/冲击环 → HUD。

公共发光使用 `lighter` 混合，但函数内部必须 `save/restore`。不要让 `globalAlpha`、`lineDash`、`lineCap` 或混合模式泄漏到后续绘制。

## Shadertoy 参考方式

当前主画布是 Canvas 2D；Shadertoy 使用 WebGL 2.0 片元着色器，依赖 `iResolution`、`iTime`、`iMouse` 和纹理通道，不能直接复制到 `CanvasRenderingContext2D`。官方着色器页面和 GLSL 帮助可从 [Shadertoy](https://www.shadertoy.com/) 查看。

当前只借鉴算法，不直接复制第三方 shader 代码：

- `sin(time + position)`：喷焰摆动、能量脉冲。
- 多层不同宽度和透明度：外焰、主色、白热芯。
- domain warp 思路：让轮廓中心随时间偏移，而不是纯直线三角形。
- SDF 思路：用“到线段/圆的距离”统一视觉边界和碰撞边界。
- 色散只用于终极攻击边缘，主体保持白芯，不做全屏 RGB 分离。

如以后增加 WebGL：新建独立透明特效层并先做性能原型，不得直接把主画布从 2D 改成 WebGL。需要保留 Canvas 降级路径，并确认低端设备稳定 60FPS 后再接入正式流程。

## 性能约束

- 禁止在敌人或子弹循环中每帧创建大尺寸渐变、离屏 Canvas 或纹理。
- 发光优先复用 `FX.sprite` 缓存。
- `draw()` 内不要使用随机数制造位置闪烁；随机只在创建特效时采样。
- 同一攻击最多保留三层主光和少量装饰粒子。
- 测试档用于快速覆盖机制，不代表可承受无限粒子；仍应遵守正式档对象上限。

## 提交前验收

- 运行 `node tests/projectile-art-smoke.mjs`，确认硬点、弹速和伤害未因美术调整改变。
- 运行全部原有回归测试。
- 分别在 `?balance=actual` 和 `?balance=test` 检查。
- 截图至少包括前摇、攻击峰值和余波各一张。
- 控制台不得出现错误或资源加载警告。
- 在 `DESIGN.md` 记录新增视觉语义和遇到的问题。
