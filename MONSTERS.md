# 怪物生成、数值与龙 Boss 设计

## 唯一数据源

- 正式/测试全局档：`js/config.js` 的 `BALANCE_PROFILES`
- 指定机制作弊场景：`DEBUG_SCENARIOS`，支持 `debug=boss` 与 `debug=midboss`
- 怪物基础数值：`js/config.js` 的 `ENEMY_DEFS`
- 自然刷怪规则：`ENEMY_SPAWN_ROSTER`
- 编队：`FORMATIONS`
- Boss 时间点：`BOSS_SCHEDULE`
- 可视化列表：`dev/monster-balance.html`

自然生成采用显式权重抽样。每种怪物同时拥有解锁时间和场上数量上限，避免突进、散射和堡垒类单位因为随机连抽形成不可读的瞬间难度。小陨石只由大陨石死亡分裂，星蚀龙只由 Boss 时间表生成，不进入普通随机池。

## 正式档生成节奏

| 时间 | 新增单位 | 作用 |
|---:|---|---|
| 0 秒 | 陨石、蜂群 | 建立移动与基础射击节奏 |
| 30 秒 | 掠袭者 | 引入有前摇的突进威胁 |
| 45 秒 | 炮手 | 引入单发弹幕 |
| 80 秒 | 连射兵 | 引入时间连续压力 |
| 115 秒 | 散射兵 | 引入扇形封位 |
| 145 秒 | 堡垒 | 引入高血量与死亡弹幕 |
| 90 秒 | 折跃棱堡 | 两阶段几何弹幕中期 Boss |
| 180 秒 | 星蚀龙 | 三阶段最终 Boss |

每波 30 秒：前 20 秒自然出怪，后 10 秒用于清场和喘息。Boss 存活期间暂停编队和自然刷怪，只允许 Boss 自身的护卫召唤进入。中期 Boss 击败后至少保留 15 秒整备；若最终 Boss 时间已经逾期，也必须等整备结束且场上普通敌人不超过 10 只才可登场。

## 折跃棱堡战斗循环

折跃棱堡负责在最终龙战前教学“观察弹幕形状与安全缺口”，不使用龙的长身体、冲刺或吐息：

| 阶段 | 触发 | 行为 | 玩家对策 |
|---|---|---|---|
| 棱镜封锁 | 生命 100%–50% | 四枚护盾棱镜绕核心旋转；周期释放留有相向安全缺口的环弹，并少量召唤蜂群 | 识别缺口方向穿环，同时清理近身护卫 |
| 核心过载 | 半血转阶段 | 护盾解体并播放 0.9 秒爆光，不进行攻击 | 利用明确停顿重新站位 |
| 过载追猎 | 生命 50%–0% | 核心在上半场高速横移，交替发射瞄准扇射和变速三轴弹幕 | 横向切出扇区，再从不同速度弹列之间穿过 |

护盾棱镜属于共享血条的减伤命中区，承受 42% 伤害；中心核心承受完整伤害。Boss 生命基于登场时玩家理论DPS估算目标战斗时长，并受基础生命倍率上下界约束，避免高DPS机炮秒杀与低DPS持续流打两分钟。击败棱堡后恢复普通流程，不触发胜利；星蚀龙仍是唯一最终 Boss。

## 龙为什么这样游动

龙不能像普通敌机一样每帧直接朝玩家转向，否则整条身体会像硬棍，也不能让每个身体节独立追踪前一节，否则会产生弹簧抖动和节距漂移。

当前方案分成三层：

1. 龙头只追逐一个围绕玩家缓慢运动的目标点，并限制每秒最大转角，因此会形成盘旋、切入和离开的曲线。
2. 记录龙头走过的世界坐标轨迹；身体不做独立 AI，而是在轨迹上按固定弧长每 27px 采样一个位置。
3. 每个身体节沿轨迹切线旋转，肩部翼绑定第二节，尾刃绑定最后一节。速度只做小幅呼吸变化，翅膀独立低幅扇动。

这种方式的关键优点是与帧率无关：30 FPS 和 144 FPS 下，身体节仍保持相同的世界距离。龙头切换行为时，身体仍复用同一条轨迹骨链，不需要每节各自运行 AI。

## 星蚀龙战斗循环与数值

| 阶段 | 持续时间 | 行为 | 玩家对策 |
|---|---:|---|---|
| 盘旋 | 6.5 秒 | 围绕玩家游动；龙头每 2.4 秒三连射；第 2、5、8、11 节身体依次每 0.9 秒双发射击 | 在龙身内外切换，寻找龙头输出角度 |
| 突击 | 0.9 秒预警 + 0.82 秒冲刺 | 锁定玩家位置显示金色虚线，然后以 470px/s 沿锁定方向冲刺 | 看到预警后横向躲避，冲刺开始后不会追踪转弯 |
| 吐息 | 1.2 秒蓄力 + 2.5 秒喷射 | 锁定方向后小幅扫射 650px，危险半宽 34px，单次有效受击 18 点 | 绕到龙头侧后方，避免沿光束方向后退 |

龙有 12 个碰撞区：1 个龙头弱点和 11 个身体区。龙头承受 100% 伤害；身体只承受 35% 伤害。一次攻击对同一条龙只结算一次，贯穿或大范围攻击不会因为同时覆盖多节而乘算 11 次伤害。玩家子弹、激光、冲撞、近战、范围爆炸和等离子尾迹都复用同一碰撞区规则。

吐息表现分为三层：蓄力阶段在龙口聚合三重能量环；喷射阶段绘制紫红外焰、红色主束和白金高温核心；沿光束移动的脉冲环与端点爆光强化速度感。伤害判定使用玩家到线段的最短距离，与视觉宽度使用同一数值。

## 龙位图资源管线

已将此前制作并通过透明通道检查的 17 个龙组件接入 `assets/dragon/`。`js/assets.js` 同时加载图集、`manifest.json` 和组件标签；绘制时必须使用 `manifest.frame_layout.rows.modules` 中的矩形，不能在运行时猜测网格。

位图只负责造型，运动仍由路径骨链负责：代码分别把头、颈部、身体模块、左右翼和尾刃绑定到对应采样点。位图尚未加载或加载失败时，自动使用同结构的 Canvas 多边形降级绘制，Boss 不会因此变成不可见对象。

## 已发现并修复的问题

- 中期 Boss 接入前，遭遇调度把 `dragon` 写成唯一分支，新增时间表条目也不会生成实体。现改为显式 Boss 工厂分派，并在已有 Boss 存活时延迟后续事件，避免玩家未及时击败棱堡时与最终龙重叠。
- 旧说明声称 Boss 期间会限制自然怪数量，但生成导演实际上仍持续自然刷怪。现统一为 Boss 存活时暂停自然刷怪和编队，仅处理 Boss 标记的护卫召唤，使弹幕安全区保持可读。
- v34 第二局中，Nova 对棱镜作战 1:59，战斗跨过最终 Boss 的 180 秒时间点，导致棱镜死亡后星蚀龙立即补触发。v35 增加 15 秒整备、入场敌人数门槛与有上下界的DPS目标生命；同时取消 Boss 登场时 1.1/1.4 秒的世界硬冻结，保留出生无敌和公告作为预警。
- `Enemy` 类中旧版方法重复定义，后出现的 `draw()` 覆盖新版 `Sprites.enemy()`，导致美术库实际未生效。已删除重复实现。
- 横列编队引用不存在的 `spitter`，抽中时整组会被解锁过滤器丢弃。已改为 45 秒解锁的 `gunner`。
- 旧龙演示页尾刺把数组坐标写成 `tp.x/tp.y`，产生无效坐标。已改为数组索引。
- 原自然怪物池通过重复 `mite` 隐式增加权重，难以看懂和维护。已改为显式权重表。
- 首轮浏览器回归出现 `weightedPick is not defined`。源码函数已经存在，实际原因是无构建静态页面复用了修改前的脚本缓存；已给入口脚本统一增加版本查询参数，使依赖更新后强制重新加载。
- 首版轨迹记录只比较相邻两帧位移；60FPS 下单帧位移小于 4px，历史锚点可能永远不插入，身体只能沿初始直线。修复为“第 0 点实时跟头、与第 1 个历史锚点距离达到 4px 时插入上一头点”，并增加 12 秒运动回归检查。
- HTML 画布仍为 `960×600`，而游戏配置、边界和出怪坐标使用 `1080×675`，导致内部坐标整体偏移。已统一为 `1080×675`，并在配置测试中锁定两处尺寸必须一致。
- 掠袭者突刺阶段曾继续朝玩家当前位置旋转，视觉朝向可能与已锁定的突刺速度不同。现在蓄力和突刺阶段固定使用 `aim` 方向。
- 三文件批量补丁第一次因大范围上下文定位失败而在校验阶段被拒绝；没有产生半写入状态。后续拆分为配置、实体、碰撞和表现四个小补丁，逐项执行语法与行为测试。
- 新三阶段测试首次沿用旧的“相邻节直线距离必须大于 24px”断言。高速突击急转处虽然仍按 27px 弧长采样，但弦长最低约 23.8px，属于曲线几何而非节距漂移；测试改为允许大于 22px，并继续锁定节点数量、有限坐标和全阶段覆盖。
- 浏览器验收时 8765 静态服务已停止，刷新后出现连接拒绝。确认源码语法和离线测试正常后，重新启动本地 HTTP 服务并完成页面回归；这不是代码运行异常。截图从浏览器隔离临时目录保存到 `dev/` 时需要单独文件权限，最终已留存盘旋与吐息两张验收图。
- 收尾检查发现 `E:\打飞机` 当前不是 Git 仓库，因此无法提供版本差异统计或回滚点；本轮只保留源码、文档、测试与验收截图。若要继续长期开发，建议下一轮先初始化 Git 并做一次基线提交。
- 旧实现把正式节奏、测试直达和 `99999` 生命分散在 `config.js`、`game.js` 的多个裸数字中，无法确认当前成绩属于哪套数值。现已拆成 `actual`、`test` 两个配置档和独立 `debug=boss` 场景；页面左下角会标记测试档，非法配置名自动回退正式档。
- 超长数值页第一次全页截图看似重复了末尾表格；DOM 检查确认每个表格和标题都只有一个，属于截图拼接伪影。改用普通视口截图留档，未对正常 HTML 做无依据修改。

## 当前边界

当前没有断节或部位独立血量：身体是共享 Boss 血条的减伤区。吐息使用玩家 0.8 秒受击无敌帧控制连续伤害频率，因此同一束吐息不会逐帧扣血；后续应根据试玩数据决定是否把吐息改为独立的固定伤害节拍。阶段数值是首轮可玩基准，仍需在完整三分钟流程中验证移动速度、弹幕密度和躲避窗口。
