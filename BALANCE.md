# 《星火战机》数值配置说明

## 为什么拆成两套

项目现在区分“正式平衡”和“开发验收”，避免为了快速看到后期怪物、Boss 或技能而把作弊数字写进正式逻辑。

- `actual`：默认配置。用于正常试玩、难度评价和以后发布。
- `test`：加速配置。用于几十秒内覆盖全部敌人、编队、升级和 Boss，不用于评价正式难度与成绩。
- `debug=boss` / `debug=midboss`：分别直达最终 Boss / 中期 Boss，关闭自然刷怪并提供 99999 生命；它们不是第三套平衡。

## 如何切换

| 用途 | 地址 |
|---|---|
| 正式完整流程 | `http://127.0.0.1:8765/` |
| 测试完整流程 | `http://127.0.0.1:8765/?balance=test` |
| 正式数值直达 Boss | `http://127.0.0.1:8765/?balance=actual&debug=boss` |
| 测试数值直达 Boss | `http://127.0.0.1:8765/?balance=test&debug=boss` |
| 测试数值直达中期 Boss | `http://127.0.0.1:8765/?balance=test&debug=midboss` |
| 数值表 | `http://127.0.0.1:8765/dev/monster-balance.html`，页面顶部可切换两档 |

没有填写 `balance`、填写错误值或直接打开首页时，一律回退 `actual`，防止测试配置误进正式试玩。

## 两档差异

| 指标 | 正式数值 `actual` | 测试数值 `test` | 目的 |
|---|---:|---:|---|
| 玩家生命倍率 | 1.0 | 3.0 | 测试时容许观察完整机制 |
| 玩家伤害倍率 | 1.0 | 2.0 | 缩短击杀与升级验证时间 |
| 敌人生命倍率 | 1.35 | 0.65 | 正式档保留杂兵分层，测试档加快清场 |
| 敌人伤害倍率 | 1.0 | 0.5 | 避免测试因操作失误过早中断 |
| 经验倍率 | 1.0 | 3.0 | 快速验证成长与技能树 |
| 普通敌人全部解锁 | 145 秒 | 29 秒 | 在半分钟内覆盖完整怪物池 |
| 中期 / 最终 Boss | 90 / 180 秒 | 18 / 36 秒 | 快速覆盖两场 Boss 战 |
| 波次 / 出怪窗口 | 30 / 20 秒 | 12 / 9 秒 | 压缩流程但保留休整结构 |
| 编队首次 / 周期 | 24 / 28 秒 | 8 / 12 秒 | 正式档提高阵型存在感，测试档快速验证 |
| 自然刷怪软 / 硬上限 | 95 / 165 | 60 / 110 | 正式档容纳更高密度，测试档不做极限压测 |

## 出怪预算制（v1.1）

自然刷怪不再只按固定间隔抽签，而是按【玩家理论输出上限】折算每波敌人总血量预算：

- 预算 = `玩家 DPS 估算 × 出怪窗口(20s) × pressure`（actual 0.75 / test 0.6）
- DPS 估算由攻击 handler 分型计算：弹丸计入伤害/弹道/射速，持续流计入直接 DPS、满层腐蚀、双焰刃或双域及周期坍缩（含全局倍率）
- 每只敌机入场时把自身 `maxHp×难度倍率` 记入已花费预算；预算用尽本波自然刷怪停止
- 每波开始随机执行一种【大批量生成方案】（MASS_SPAWNS，占预算 55%/60%）：
  蝗潮宽弧 / 陨石斜带 / 蜂群双螺旋 / 爆虫楔形 / 弹幕横墙 / 混编突击，数量按预算缩放（4~30）
- Boss 存活期间：编队与自然刷怪暂停，只处理 Boss 主动创建的护卫请求，保证机制可读
- 玩家升级后 DPS 估算上升 → 下一波预算自动提高；清场过快仍有 5 倍加速刷新
- 阵型只在出怪窗口内、预算未耗尽、Boss 不在场且上一组阵型已完成排队时调度；最近两次使用过的阵型暂不参与抽取
- 容量统计同时计算场上和待生成单位；自然/阵型/批量来源的待生成上限分别为 12/32/36

## 数值的三个层级

1. `ENEMY_DEFS`、武器 `levelStats`、技能树 `fire`：单位的基础设计数据。两档共用，保证测试的仍是同一种怪物和武器。
2. `BALANCE_PROFILES`：全局倍率和流程节奏。只在这里定义正式档与测试档差异。
3. `DEBUG_SCENARIOS`：针对某个机制的作弊场景，例如 Boss 直达和 99999 生命。不得用于正常成绩。

## 修改规则

- 调整正式难度：先修改 `actual` 或基础单位数据，再用完整三分钟流程验证。
- 只想更快看到内容：修改 `test`，不要改基础怪物和武器数据。
- 新增调试作弊：放入 `DEBUG_SCENARIOS`，不能在 `game.js` 写裸数字。
- 两档都必须运行 `node tests/balance-profiles-smoke.mjs`、`node tests/balance-smoke.mjs` 和相关机制测试。

## v34 第一局反馈修正（2026-09-02）

第一局正式监控结果为：机炮弹幕流 3:02 通关、Lv12、302 击杀、0 实际受伤；棱镜战 8 秒、星蚀龙战 2 秒，敌人/敌弹峰值仅 22/25。由此确认不是单点 Boss 问题，而是后期输出成长、敌方生命与出怪并发的曲线失配。

- 正式档普通敌人生命倍率由 1.0 提至 1.35，时间成长由每秒 0.005 提至 0.009、每波成长由 0.02 提至 0.05；保留蜂群可秒杀，但让射手与重装单位进入 2 次以上命中区间。
- 正式档自然生成改为每组 2~4 只，缩短刷新间隔，提高预算压力与容量上限；增加的是并发与组合，不移除出生预警。
- 棱镜与星蚀龙增加独立生命倍率 1.6 / 6，目标是至少完整展示主要阶段；不通过提高伤害制造假难度。
- 自动索敌改为约 9 rad/s 的限速转向并保留当前目标：180° 转向约 0.35 秒，仍灵敏，但不再瞬间掉头。
- 普通敌人位于屏外时不再成为自动索敌目标；屏外击杀的经验会投放到最近的画面边缘，经验晶体自身也被约束在可见区域。
- 监控新增平衡修订号和场外击杀计数。第一局作为 v33 基线保留，第二、三局会记录为 `v34-feedback-1`，因此本轮主要验证修正方向，不直接拿三把做最终武器强弱排名。

实施时全量测试首先在 `balance-profiles-smoke` 失败：正式编队首次出现已从 35 秒改为 24 秒，但旧测试仍固定断言 35。修复方式是同步更新该配置契约；这不是运行逻辑或路径拆分问题，而是测试正确阻止了“改配置却忘记改预期”的不一致。

当前 `actual` 仍是试玩基准，不代表最终上线平衡。后续应依据完整局时长、死亡时间分布、武器 DPS 和 Boss 命中率逐步调参；测试档成绩不能与正式档成绩比较。
