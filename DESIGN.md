# 《星火战机》框架设计 v1.0

网页端肉鸽弹幕射击（横板）。复刻 Nova Drift 的扁平几何霓虹构成，
土豆式八方出怪，**双轨成长**：数值三选一 + 蜂巢特效技能树（大树化）。

## 数值环境

运行时默认使用 `actual` 正式数值；`?balance=test` 使用加速测试数值；`debug=boss` 只是直达 Boss 的调试场景。两档差异、切换地址和修改规则见 `BALANCE.md`。测试档成绩不得用于评价正式难度。

## 艺术与特效协作

公共视觉语法、流派特效时间结构、Shadertoy 借鉴边界及 GLM 编码要求见 `GLM_ART_HANDOFF.md`。造型硬点必须同时驱动可见炮口和真实弹道，禁止表现与判定各写一套坐标。

## 攻击框架 v2（v22）

- `js/attacks.js` 是攻击类型注册与校验入口：统一 `update / draw / validate / estimateDps` 契约，以及 `muzzle / bodyScatter / center` 出生点策略。游戏启动时校验正式技能树与实验武器配置，字段缺失直接报错。
- 标准攻击（机炮、激光、冲撞、近战、尾迹）通过轻量适配器复用原有战斗实现；焰刃、毒雾、疫焰和孢子使用独立 handler。公共层统一生命周期，不把所有机制塞进一个分支函数。
- 每段保留攻击使用独立 `AttackRuntime`，键为“流派、形态、链位置”。冷却、雾粒、焰刃角度和坍缩计时不再共享，避免进化后前一级攻击覆盖新形态状态。
- 技能树显式声明进化语义：机炮和 DOT 为“保留前级攻击、继承链上升级”，相位刃为“新形态替换旧攻击、只用当前升级”。
- 腐蚀孢子是特殊生成逻辑：从机体周围 `8–26px` 随机释放，先以基础弹速的 `18%–28%` 漂移；敌机进入 `150px` 后武装追踪并加速，单段最多保留 10 枚。它不是炮口直射，也不是无目标时停火。
- DPS 估算按攻击类型计算，已覆盖持续伤害、满层腐蚀、双焰刃/双域和周期坍缩；普通激光等非技能树武器不再得到空值。属性改变后立即刷新，下一波敌人血量预算使用最新结果。
- 同步修复两项独立问题：混编楔形的虚拟类型 `mixed` 曾被当成敌机定义读取而崩溃；大批量敌人在“排队预警”和“真实入场”各记一次血量导致预算双扣。现在混编先换算具体类型，且只在真实入场时记账。
- `tests/attack-framework-smoke.mjs` 覆盖注册表、全配置校验、非法孢子配置、非树武器 DPS、保留型进化和混编预算；`archetype-art-smoke.mjs` 追加孢子的漂移、近距武装、加速和 10 枚容量测试。
- 实机验收剩余警告：`js/assets.js` 加载龙位图时浏览器报告无法解码，当前会自动使用 Canvas 龙，不影响战斗；后续需单独检查龙图文件格式与位图导出管线。

## 系统规范 v23：增量进化、光盾与出怪 Director

### 规范边界

- 规范化的是生命周期、状态归属、配置校验、指标与调度边界，不要求所有攻击共享一种运动方式。孢子近距武装、焰刃锁敌、毒雾驻留等特殊行为仍由独立 handler 负责。
- `progression.attacks: retain` 的含义固定为“进化新增攻击实例，旧实例继续运行”；每个实例保留 `formId / parentFormId / evolutionPath / AttackRuntime`。`replace` 只用于明确声明覆盖式进化的技能树。
- 最终形态必须能够读取父分支。毒雾 → 疫焰生成 `orbit` 双刃，独立于机头以 1.65rad/s 公转；焰刃 → 疫焰生成 `targetLock` 双刃，在 560px 内平滑瞄准最近敌人。两条路线都保留孢子与中间攻击。
- `debugForm` 仅用于隔离单个形态美术，不代表真实战斗；机制验收必须使用合法 `debugPath`，例如 `base,mist,plague` 或 `base,flameblade,plague`。

### 一次性光盾

- `ShieldRuntime` 分离剩余层数、0.16s 同碰撞簇保护、受击方向、破碎计时和回充计时。光盾抵消伤害时不触发生命无敌帧，但同一团重叠弹丸不会在一个瞬间连续剥掉多层。
- 视觉分为常驻薄膜、层数轨道、方向性偏转面/裂纹与回充脉冲；受击源坐标由敌机、敌弹和 Boss 传入，表现方向与真实碰撞一致。
- HUD 缓存签名包含当前护盾层数，消耗或恢复后立即更新，不再长期显示旧值。

### 阵型与出怪

- `js/spawns.js` 定义统一 `SpawnRequest`：敌机类型、入口角、横向偏移、延迟、来源、生成组、优先级与重试次数。自然刷怪、批量方案和阵型都从同一入口排队。
- `SpawnDirector` 同时统计场上与待生成单位，按来源队列上限、全场硬上限和单类型上限拒绝过量请求；临时满场时最多短暂重试 8 次，不再静默丢失一次调度。
- 阵型配置必须声明 `id / role / category / weight / unlockAt / build`。选择时排除最近两次阵型，避免连续重复；Boss、波末休整、预算耗尽或已有阵型待生成时暂停新阵型调度。
- 同一生成组在相近入口的预警会聚合并显示数量，解决墙阵与楔阵多个标记完全重叠、亮度失控的问题。
- 作用范围包括自然刷怪、`MASS_SPAWNS` 和 `FORMATIONS`；Boss 时间表仍是独立脚本事件，因为它需要清场、公告和专用实体，不应伪装成普通阵型。

### 战机与弹道审查记录（v15）

- 修复“炮弹从机身内部出现”：`Sprites.playerMuzzles` 现在同时提供可见炮口与世界坐标弹丸出生点。
- 修复终极弹三色尾迹方向错误：弹丸局部坐标以前进方向为 `+x`，所有尾迹统一向 `-x` 延伸。
- 修复形态表现不一致：普通、散裂、磁轨与终极形态分别使用 1、2、1、3 个喷口；尾焰统一走 `FX.energyPlume`。
- 修复激光从机体中心出射：光束起点改为当前形态的真实炮口。
- 子弹速度、伤害、贯穿和同目标去重逻辑保持不变；新增 `tests/projectile-art-smoke.mjs` 防止后续美术改动破坏弹道。
- 当前渲染仍为 Canvas 2D。Shadertoy 只借鉴分层白芯、时间正弦、轮廓扭曲和距离场思路；是否引入 WebGL 透明特效层需另做 60 FPS 性能原型。
- 视觉验收页：`dev/sheet-ship.html`、`dev/sheet-bullets.html`；实机与图鉴截图保存在 `dev/*-v15.png`。

### DOT 完整进化线（v21）

- 纠正上一版“一个基础新星等于一条流派”的过度简化：正式结构为 1 个原型机、2 个中间分支和 1 个终极机，共 4 套机体与攻击轨迹。
- `腐蚀孢子`：在机体周围持续释放的低直伤孢子；先漂移，敌机靠近后再追踪，命中建立腐蚀层，初始阶段无需贴近敌群。
- `连续焰刃`：橙色双喷口机；沿机头持续喷射贝塞尔焰锋，伤害判定使用同长度、同宽度的线段区域。
- `粒子毒雾`：青色多孔雾化机；在世界坐标生成驻留软粒子，圆形领域内持续伤害并叠腐蚀。
- `疫焰天灾`：三喷口终极机；同时维持双旋焰刃、粒子雾与周期坍缩。
- 蜂巢布局从写死 `shotgun/rail/ultimate` 改为读取 `branchForms` 与 `finalForm`，后续武器可定义自己的双分支名称。
- 本地可用 `?debugForm=flameblade|mist|plague` 直达指定形态进行美术验收；非 localhost 域名忽略该参数。
- 实机验收曾发现火力面板把所有武器都按“子弹伤害 × 弹道数”计算，导致新星显示 `NaN`；现已按攻击模式展示脉冲、焰刃、雾域和双域指标。
- v20 的 95px 径向脉冲迫使初始机贴脸，既不符合飞行射击的安全距离，也与后续焰刃分支争夺定位。v21 改为远程孢子弹，保留焰刃的中近距持续输出和毒雾的范围控场。
- “纳米维修”与 DOT 建立受控联动：有效恢复速度在 `5.0/s` 软封顶；每 `+1.0/s` 恢复增加 12% 腐蚀层伤害和 5% DOT 射程/半径，最多分别增加 60% 与 25%。回血与输出共用成长资源，但不会无限膨胀。
- 孢子弹在发射时冻结腐蚀层快照，命中后再结算；避免飞行途中切换形态或恢复属性变化造成同一弹体结果漂移。裂孢只向 48px 内传播腐蚀，不复制直伤。
- 测试问题记录：孢子改为真实弹体后，`archetype-art-smoke` 首次报 `Bullet is not defined`。根因是旧测试夹具只加载武器脚本，近身脉冲从不依赖实体类；补载 `js/entities.js` 后由同一测试直接验证真实弹体的腐蚀快照方法，未在生产代码中增加兼容分支。

## v1.0 变更（GPT 协作轮后）

权威文档以 `GLM_ART_HANDOFF.md`（视觉语法/管线规范）、`BALANCE.md`（双数值档）、
`MONSTERS.md`（怪物节奏与星蚀龙 Boss）、`BULLETS.md`（子弹图鉴）为准，本文件不再重复。
本轮新增：星蚀龙 Boss（节段跟随链+12 碰撞区+三阶段攻击）、腐蚀孢子四形态流派
（孢子/焰刃/毒雾/疫焰）、双数值档 actual/test、焰刃转向与脉动规范、
DoT 快速叠毒、链式效果继承（沿进化链回溯，不全局池化）。

## v0.9 变更

- **精灵库 `js/sprites.js`**：玩家机/8 种敌机/4 类子弹的绘制唯一来源，
  游戏与 `dev/sheet.html` 美术参考表共用（先看参考图再进游戏）
- **位图资源管线 `js/assets.js`**：读取 sprite-gen 图集 manifest 与语义标签，
  星蚀龙使用独立头/颈/身体/翼/尾刃组件沿路径骨链组装，加载失败时回退 Canvas 绘制
- **数据化刷怪表**：自然怪物统一配置解锁时间、抽样权重和场上上限；
  `dev/monster-balance.html` 直接读取运行时配置展示生成与数值列表
- **敌机 roster 重做**：开局陨石（缓慢漂移、死亡分裂）+ 直线蜂群；
  弹幕怪按【单发炮手 45s → 串射连射兵 80s → 散射兵 115s】递进
- **连射兵 bug 修复**：串射启动未重置冷却导致子弹连成一条线
- **碰撞双向化**：玩家撞击敌机造成碰撞伤害并互相推开（敌 20px/玩家反冲 7px）
- **流动星河背景**：预渲染星云（5 色斑+尘带）纵向无缝流动
- **美术升级**：玩家层叠机甲（板线/座舱/翼灯）、敌机双层描边+内部细节
  （陨石坑/炮塔内环/旋转堡垒环/花瓣发射器）、子弹分类外形
  （普通=泪滴彗星、强力=尖头镖+激波线、爆裂=脉动橙球+引信火花、碎片=追踪小点）

## v0.8 变更

- **右侧火力面板**：实时显示 伤害×弹道 / 射速(间隔) / 单体DPS / 特效标签，
  让"射速换弹幕"的进化取舍透明化（回答"升级后攻速降低"：进化形态
  0.46s→0.62/0.8s 射速下降，但 DPS 20→79/113 上升）
- **数值卡加强**：伤害 8→12%、射速 7→10%、船体 10→14、推进 6→9%、
  磁力 20→25%、维修 0.4→0.6（缩小与蜂巢质变的体感差距）
- **特效升级**：瞄准光标改为旋转四段弧括弧；尾迹缎带尾端收细
- 好特效原则（文档）：①传达信息 ②代言数值 ③三层分离 ④短促消散 ⑤攻击-命中-击杀反馈闭环

## v0.7 变更

- **波次呼吸**：每波 30s，前 20s 持续出怪、后 10s 休整（SPAWN_WINDOW）
- **数值三选一防刷新**：选卡期间进入 levelup 暂停态，连升多级只重建一次卡
- **阵型去文字化**：不再显示编队名称，靠边缘预警 + 阵形可读
- **蜂巢大树**：六边形缩小（r=26），基础簇在底部、进化簇在上方左右，
  虚线进化路径相连，一张图看完整成长路线，由最低级逐步向上点亮
- **HUD 沉底**：血条/经验条移至底部居中，等级+属性条在左上
- **特效重做**：尾迹改为连续缎带（粗线连接+内芯亮线）、引擎尾焰为抖动焰舌、
  鼠标模式新增瞄准十字光标
- **画布基准放大**：960×600 → 1080×675（移速 275→300 同步），界面排版以此为基准
- **蜂巢树排版修复**：全格统一尺寸无缝密铺（原中心格偏大导致重叠）、集群紧凑化、
  集群名称下移不压格、树状态下隐藏 DOM HUD
- **三簇蜂巢拼成一块 19 格大蜂窝**：基础簇 7 格 + 两个进化簇各 6 格严丝合缝，
  进化精华格正好占据簇间咬合位（做蜂巢的本意）
- **清场加速**：场上清空时无视休整窗口、刷怪计时 5 倍速，快速开下一波
- **突刺者限量**：场上最多 5 只；**怪物特效统一红色系**（蜂群正红/掠袭者橙红/
  散逸体玫红/裂殖体绯红/堡垒深红），与玩家冷色阵营对峙
- **平衡修复**：重弹伤害漏乘 ×2（DPS 19→40）；DPS 实测建档：
  白板 17 / 基础满特效 40 / 散裂满特效 55-129 / 磁轨满特效 25-113（蓄力条件）

## v0.6 变更（参考 Nova Drift Wiki 敌人构成与行为设计）

- **双轨成长**：每级 = 数值三选一（伤害/射速/船体/推进/装甲/牵引/维修）+ 1 蜂巢技能点；
  升级曲线加快（L1=8, L2=12, L3=17）
- **蜂巢节点全部特殊效果化**（不再纯数值）：
  机炮：过载弹芯（溅射）/ 弹射机制（弹向最近敌机）/ 侧弦击发（±24° 副弹）/
  终极·火控中枢（每 3 发重弹+击退）；散裂弹幕：碎裂/爆裂/蜂群制导（追踪）；
  贯穿磁轨：蓄能狙击（静止充能 ×2.5）/ 电磁脉冲（自动清弹）/ 湮灭磁轨（链式闪电）
- **怪物节奏重排**：蜂群 0s → 掠袭者 25s → 裂殖体 55s → 散逸体（三连弹）85s →
  堡垒 120s；刷怪间隔 1.5s 起步缓升至 0.7s；编队首次 35s、周期 40s，
  且编队敌机类型同样受解锁表约束
- **界面按 Nova 构成方式重铸**：暗面板 + 青色细线 + 角标括弧 + 切角按钮 + 左色条卡片

## v0.5 变更

- **双操作模式**：WASD = 手动朝向（机头随移动方向，射击沿机头）；
  鼠标/触屏 = 位置跟随 + 自动索敌
- **标准蜂窝树**：平顶六边形密铺，中心紧贴环绕 6 格——正上=终极（需下方 3 格全亮）、
  上方左右=精华方向（改变攻击形态）、左下/正下/右下=强化；每形态 3 强化 + 终极
- **主菜单流程**：死亡结算 =「再来一局 / 返回主菜单」；暂停中按 Q 回主菜单
- **稳定性修复（"莫名中断"根因）**：敌机无上限堆积（3 分钟 645 只）导致帧率雪崩——
  现在自然刷怪软上限 70、全场硬上限 120、刷怪间隔放缓；
  发光特效改为预渲染精灵图（不再每帧 createRadialGradient）；
  主循环 try/catch 兜底，单帧异常不再杀死 rAF 循环（错误横幅提示）

## v0.4 变更

- **移除抽卡**：升级 = 1 技能点，手动点亮蜂巢技能树（T 打开，升级自动弹出）
- **蜂巢结构**：中心=当前攻击形态；下方 4 格=强化（任意顺序）；正上方=终极
  （需 4 格全亮）；上方左右两格=**精华方向**（需终极，改变攻击形态成为新中心，
  拥有自己的 4 强化 + 终极，树可继续生长）
- **机炮全树实装**：脉冲机炮 →（散裂弹幕：五向散射 / 贯穿磁轨：贯穿重弹），
  各 4 强化 + 终极；其他武器树施工中（开局不可选）
- **射击朝向**：机头始终朝准心（鼠标/触屏），子弹沿朝向发射
- **右键挥刀**（副武器）：4s 冷却，清除 175px 内全部敌弹 + 范围伤害击退
- **怪物曲线放缓**：血量 = 1 + 时间×0.005 + 波次×0.02（30s 约 1.08 倍，原 1.4 倍）
- 磁轨溅射弹头：命中溅射半径伤害（Bullet.splash）

## 操作

鼠标/触屏/WASD 移动；机头朝准心自动开火；右键挥刀清弹幕；T 技能树；Esc/P 暂停。

## v0.3 变更

- **横板化**：画布 960×600，玩家居中
- **八方出怪**：敌机不再单一方向刷新，而是以玩家为中心从任意八方向屏幕边缘包抄；
  入场前在边缘显示脉冲预警环 + 指向玩家的箭头（`edgeSpawnPos` + 预警队列）
- **初始界面 BP**：左栏 6 武器选 1；右栏 4 流派「禁用 1 个 + 选定 1 个」，
  流派改变初始属性与升级卡权重（弹幕密度 = 武器卡×2，机动游走 = 属性卡×2 等）
- 编队全部改为方向制：V字 / 八方包围 / 横列推进 / 螺旋涌入 / 两翼夹击

## 技术选型

- 原生 JavaScript + Canvas 2D，零依赖、零构建，纯静态可部署
- 所有武器、敌机、编队、升级卡均为数据驱动（配置表 → 行为分发）

## 特效框架（v0.2 核心，先于一切内容）

- **发光渲染管线**：三层绘制——叠加发光底层（尾迹/晶体/敌机光晕）→ 实体本体（暗填充+霓虹描边）→ 叠加发光顶层（子弹/光束/粒子/冲击环），全部走 `lighter` 混合
- **FX 工具**（js/fx.js）：径向光晕 glowCircle、描边光环 glowRing、冲击波 Ring、爆闪 Flash
- **怪物特效**：出生传送门（淡入+收缩环）、受击白闪、死亡爆闪+冲击环+火花、
  掠袭者蓄力前摇（渐亮+瞄准线）、突刺拖尾、散逸体开火环、堡垒死亡弹幕
- **子弹特效**：玩家弹 = 光晕+白热核+流光拖尾；敌弹 = 脉动光晕球；激光 = 宽晕/主束/白芯三层 + 命中火花

## 怪物图鉴（自然刷新 + 周期性规律编队）

| 怪物 | 外形/颜色 | 行为 |
|---|---|---|
| 蜂群 | 小三角·红 | 成群蛇形扑向玩家 |
| 掠袭者 | 菱形·橙 | 蓄力锁定（前摇特效）→ 高速突刺 |
| 散逸体 | 五边形·紫 | 保持距离环走，三连荧光弹 |
| 裂殖体 | 六边形·青绿 | 缓慢逼近，死亡分裂成蜂群 |
| 堡垒 | 八边形·蓝 | 重装坦克，死亡环形弹幕爆发 |

**周期性编队**（每 25s，首次 18s，带屏幕预警）：
V字蜂群冲锋 / 掠袭者环形包围 / 弹幕横列 / 螺旋涌入。

## 当前武器与成长框架

- 开局从 `PLAYABLE_WEAPON_IDS` 选择 1 门正式武器，本局成长围绕对应蜂窝技能树展开。
- 每次升级获得属性三选一和 1 个技能点；技能点用于强化当前形态、解锁分支或进入最终形态。
- `retain` 路线会保留前置攻击并新增攻击；`replace` 路线会明确替换当前攻击。
- 正式武器为脉冲机炮、腐蚀孢子和相位刃；激光、冲撞、尾焰保留为实验原型，不出现在正式菜单。
- 脉冲机炮的散裂、磁轨两路均可进入湮灭星舰；相位刃的环刃、猎杀两路均保留基础弧斩，并最终新增相位刃冠。
- 旧版“武器 Lv1→5、复制两门后融合”方案已经停止使用，不属于当前运行架构。

## 操作

鼠标跟随 / 触屏拖动（Pointer Events 统一处理）/ WASD；武器全自动；P 暂停。

## 架构

```
index.html / style.css    页面 + 霓虹 UI（选武器、HUD、升级卡）
js/utils.js               数学/随机/线段距离
js/fx.js                  发光特效层：glowCircle/glowRing/Ring/Flash
js/config.js              配置表：敌机图鉴、正式/实验武器、技能树、属性卡、编队
js/run-state.js           一局状态、合法迁移与迁移审计
js/weapons.js             武器行为分发（gun/laser/ram/nova/sword/trail）
js/entities.js            Player（武器外观联动）/ Enemy 状态机 / 弹体 / 尾迹
js/game.js                主循环：自然刷怪+编队、碰撞、发光渲染管线、卡片生成
js/ui.js / js/main.js     UI 与输入
```

## 路线图

- 当前：特效管线、11 种普通敌人、10 种阵型、3 种正式初始武器、双档数值和星蚀龙 Boss。
- 已完成：三种正式初始武器的完整进化路径、一局状态机、最终胜利结算与 localhost 自动试玩。
- 下一阶段：拆分成长/碰撞/生成/Boss 职责并增加中期 Boss。
- 后续：音效、粒子对象池、引导、完整流程测试与静态部署。
