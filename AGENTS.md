# Starfall 项目接手规则

## 项目定位

原生 JavaScript + Canvas 2D 的浏览器 Roguelite 射击游戏原型，核心是可叠加武器进化、阵型出怪和多阶段 Boss。

## 运行与验证

- 在项目根目录运行 `python -m http.server 8765`。
- 正常入口为 `http://127.0.0.1:8765/`；没有参数时必须使用 `actual` 正式数值。
- 测试档、直达 Boss、最终形态和遥测只能通过 README 列出的查询参数启用。
- 测试使用 Node.js 内置能力，无 npm 依赖；修改后运行 `tests/*.mjs` 中全部脚本。

## 技术与目录

- `index.html`、`style.css`：页面入口与 UI。
- `js/`：游戏循环、实体、攻击、碰撞、成长、出怪、Boss、遥测与特效。
- `assets/`：位图与精灵图；`dev/`：本地调试和预览；`tests/`：机制契约测试。
- `ARCHITECTURE.md` 是代码职责的权威说明，数值和试玩事实分别维护在 `BALANCE.md` 与 `PLAYTEST.md`。

## 约定

- 进化默认保留已获得攻击；升级节点应用于继承链时，专属数值字段必须先做有限值检查。
- 腐蚀层是敌人共享状态，叠层、满层引爆和 DPS 估算不得按攻击段重复实现或重复计数。
- `actual` 与 `test` 必须分离；调试作弊不得写入正式基础数据。
- 视觉调整不得改变碰撞、伤害或弹速；重要画面需浏览器验收，截图不能替代机制测试。
- 不提交密钥、遥测试玩数据、临时暂存副本或本地服务文件。

## 当前状态与下一步

当前为 v36 可通关开发版：两场 Boss、三种完整武器树、双毒圈与满层腐蚀结算均已实现。下一阶段只需继续正式档 6–8 分钟节奏验证，再补音效、新手引导和浏览器端完整流程测试。
