# Starfall 项目整体规范

本文档是 Starfall 代码结构与玩法实现的权威约束。设计稿可以描述未来目标，但运行代码、测试和本文档发生冲突时，应先确认真实行为，再同步三者，禁止让多个旧方案长期并存。

## 1. 产品边界

Starfall 是一款浏览器 Canvas 2D Roguelite 射击游戏。当前 Demo 的目标是一局 6–8 分钟的完整体验：选择机体与流派、战斗成长、中期 Boss、最终龙 Boss、胜利或失败结算、重新开始。

当前正式开局武器由 `PLAYABLE_WEAPON_IDS` 唯一决定。仅存在于数值表、攻击函数或美术稿中的武器属于实验内容，不得自动出现在正式菜单，也不得写成已完成功能。

## 2. 分层与依赖方向

代码按以下五层组织，依赖只能从上层指向下层：

1. **数据层**：武器、技能树、敌人、阵型、Boss 时间表、数值档位；只描述规则，不保存本局状态。
2. **规则层**：攻击注册、索敌、碰撞、持续伤害、成长、生成预算；接收数据并执行确定性规则。
3. **实体层**：玩家、敌人、Boss、子弹、掉落物；只保存实体状态和局部行为。
4. **流程层**：游戏状态机、计时、暂停、升级、Boss 登场、胜负结算。
5. **表现层**：Canvas 绘制、粒子、音效、HUD、菜单；不得决定伤害、掉落或生成结果。

表现层不能反向成为业务规则来源。例如“画出了护盾”不等于拥有护盾层数；真正状态必须在实体或规则层中。

## 3. 唯一事实源

- `PLAYABLE_WEAPON_IDS`：正式可选武器。
- `SKILL_TREES`：正式武器成长、形态和进化关系。
- `ATTACK_HANDLERS`：攻击类型的执行与绘制入口。
- `ENEMY_DEFS`：普通敌人的基础数值和角色定位。
- `FORMATIONS`、`spawns.js`：阵型数据与生成调度。
- `BALANCE_PROFILES`：测试数值和正式数值的切换入口。
- `BOSS_SCHEDULE`：Boss 登场安排。
- `RunState`（待提取）：一局游戏当前处于什么阶段。

禁止再建立功能相同的第二套升级表、融合映射或隐藏入口。实验内容必须显式标记，并与正式列表隔离。

## 4. 武器与成长规范

每个正式武器必须具备：

- `WEAPON_DEFS` 中的身份、名称和基础类型；
- `SKILL_TREES` 中的 `base` 形态；
- 每个形态可解析的攻击配置；
- 已注册的攻击处理器；
- 所有进化目标真实存在；
- 明确的攻击继承策略。

进化策略只允许两种：

- `retain`：升级新增攻击，旧攻击继续存在。DOT 流和组合武器默认使用该策略。
- `replace`：新形态替换旧攻击，仅适用于明确设计为武器换型的路线。

攻击的生成位置、索敌方式、伤害判定和视觉表现应分开描述。孢子随机生成、环绕焰刃等特殊行为通过独立 handler 实现，不强行塞进通用子弹逻辑。

## 5. 出怪与 Boss 规范

普通敌人定义“单体是什么”，阵型定义“如何组合”，生成导演决定“何时、从哪里、生成多少”。任何系统不得绕过生成请求直接无限创建敌人。

生成必须遵守：预警时间、来源标记、软上限、硬上限、预算和近期阵型防重复。

Boss 是独立遭遇，不按普通敌人简单放大。每个 Boss 必须具备登场、阶段切换、攻击预警、死亡回调和结算结果。Demo 采用两个 Boss：中期 Boss 两阶段，星蚀龙作为最终 Boss 三阶段。

## 6. 一局状态机

当前统一为：

```text
menu -> playing -> levelup/tree/paused -> bossIntro -> playing
     -> victory | gameover -> retry/menu
```

只有 `playing` 可以推进战斗计时、自然出怪和攻击冷却。升级、技能树、暂停、Boss 演出与结算期间是否更新时间，必须由状态机明确决定，不能分散写在多个条件中。

## 7. 数值规范

- `actual`：面向正常游玩的目标数值。
- `test`：缩短流程、降低验证成本，但不改变攻击机制和敌人身份。
- 调试参数只能在 localhost/127.0.0.1 生效。
- 测试档不能反向覆盖正式档，也不能把测试血量、无限生命写回基础定义。
- 数值修改必须同时记录目的、受影响对象和验证结果。

## 8. 测试门禁

每次提交至少满足：

1. 所有既有 smoke 测试通过。
2. 正式武器边界、技能树引用和攻击 handler 校验通过。
3. 修改攻击时，测试“旧攻击是否保留”和“特殊行为是否仍存在”。
4. 修改出怪时，测试预算、上限、预警和来源。
5. 修改 Boss 或流程时，测试单次触发、阶段顺序和胜负结算。
6. 重要视觉改动保留浏览器截图，但截图不能代替规则测试。

`dev/autoplay.js` 提供 localhost 专用完整流程冒烟测试，当前覆盖菜单开局、暂停/恢复、技能树往返、升级选择、Boss 入场、击杀和胜利结算。后续把它接入无头浏览器持续集成，并补重开断言。

### 已解决的流程测试问题

- 自动玩家最初只会移动和击杀 Boss，获得经验后停在 `levelup`。修复为按运行状态决策：升级时选择属性；并专门验证 `levelup -> tree -> levelup` 不会绕过选择。
- 浏览器曾同时运行新版 HTML/自动玩家与缓存中的旧版 `game.js`，表现为 Boss 已死亡但没有胜利页。根因不是结算算法，而是多个核心脚本沿用同一缓存版本号。统一更新所有脚本版本后恢复；今后跨文件运行时变更必须整体提升资源版本。
- 自动化结果原本只写控制台，不利于外部测试读取。现在通过 `documentElement.dataset` 发布 `running/finished/failed` 状态、状态迁移和失败堆栈，使测试脚本自身失效也可观测。
- 首次在隔离暂存区运行拆分测试时缺少 `utils.js` 等只读依赖，出现 `ENOENT`。这是测试环境组装不完整，不是生产逻辑错误；补齐真实依赖后原测试通过，未向生产代码加入兼容分支。

## 9. Game 子系统边界

`Game` 保留生命周期、每帧调度、通用特效和绘制；其余职责通过原型组合接入，维持原生脚本、零构建运行：

- `game-spawning.js`：生成请求、预算、自然刷怪、大批量方案与阵型。
- `game-collision.js`：玩家/敌人/弹体碰撞和最近目标查询。
- `game-encounter.js`：Boss 调度、入场冻结、敌人死亡副作用与胜利结算。
- `game-progression.js`：经验、属性选择、技能树往返和成长倍率。

子系统只能通过 `Game` 的公开运行数据协作，禁止互相直接持有状态副本。`game-systems-smoke` 同时检查方法归属、组合结果和 `game.js` 行数上限，防止实现重新回流到主类。

Node `vm` 测试中的对象来自独立 Realm。跨 Realm 数组不要直接使用 `deepStrictEqual` 比较原型，应比较序列化结果或稳定标量，避免把测试运行环境差异误判为玩法故障。

## 10. Git 与变更规范

- `main` 始终保持可运行、测试通过。
- 架构整理与玩法新增分开提交。
- 每个提交只解决一个可说明的问题。
- 删除旧系统前先证明其入口不可达，并清点残留引用。
- 禁止在没有测试基线的情况下同时移动文件、重命名接口和修改数值。

推荐提交类型：`refactor`、`feat`、`fix`、`test`、`docs`、`art`、`balance`。

## 11. 当前迁移顺序

1. 建立正式武器边界，隐藏未完成原型并增加启动校验。
2. 清理不可达的旧等级升级和二合一融合系统。
3. 为技能树增加完整性测试，处理机炮磁轨断头和相位刃未完成问题。✅
4. 提取状态机、成长、碰撞、生成和 Boss/结算职责。✅
5. 完成中期 Boss 与正式 6–8 分钟闭环。

当前阶段不切换引擎、不引入构建工具、不全面改写 ES Modules，也不为追求文件行数进行无收益拆分。
